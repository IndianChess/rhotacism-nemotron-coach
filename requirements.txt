gradio[oauth]==5.50.0
openai==2.36.0
huggingface_hub==1.14.0
transformers==5.8.1
torch==2.11.0
librosa==0.11.0
soundfile==0.13.1
praat-parselmouth==0.4.7
phonemizer==3.3.0
python-dotenv==1.2.2
pocket-tts==2.1.0
# Local coach backend (COACH_BACKEND=local). CUDA 12.1 wheel for Zero GPU.
# Local Mac dev uses the regular PyPI wheel (Metal). For CPU-tier Spaces,
# swap "cu121" back to "cpu".
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
llama-cpp-python==0.3.28

# Zero GPU runtime
spaces
