gradio[oauth]==5.50.0
openai==2.36.0
huggingface_hub==1.14.0
transformers==5.8.1
librosa==0.11.0
soundfile==0.13.1
praat-parselmouth==0.4.7
phonemizer==3.3.0
python-dotenv==1.2.2
pocket-tts==2.1.0
# Local coach backend (COACH_BACKEND=local). Use the CPU wheel on Spaces
# because the ZeroGPU base image does not expose libcudart to llama.cpp.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.28

# ZeroGPU runtime
spaces
